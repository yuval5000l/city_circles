import {ListItem, ListItemText, Stack} from "@mui/material";

export default function StyledSocialIcons(business){
// TODO: build a function that return the social contact possible from the business api
    return(
        <Stack dircetion="row" spacing={1}>
        </Stack>
    );
}